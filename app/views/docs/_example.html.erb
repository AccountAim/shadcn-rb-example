<div class="space-y-3">
  <%= sui.card(class: "p-6") do %>
    <div class="flex flex-wrap items-center gap-3">
      <%= capture(&block) %>
    </div>
  <% end %>
  <%= sui.codeblock code, syntax: syntax %>
</div>
